package org.team1540.kingbass.subsystems;

import edu.wpi.first.wpilibj.command.Subsystem;

/**
 * A dummy system to avoid running multiple VibrateController commands at the same time.
 */
public class Controller extends Subsystem {
  @Override
  protected void initDefaultCommand() {}
}
